doctrine:
    dbal:
        default_connection: dplan
        connections:
            dplan:
                driver: pdo_sqlite
                path: "%kernel.cache_dir%/__DBNAME__.db"
                charset: UTF8
                memory: false
                user:    test
