trigger: none


pool:
  vmImage: ubuntu-latest

steps:
- checkout: self


- script: |
   git checkout -b release
  displayName: 'Run OpenCode mirror'
  workingDirectory: $(System.DefaultWorkingDirectory)