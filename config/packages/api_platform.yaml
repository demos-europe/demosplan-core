api_platform:
    title: demosplan API
    version: 3.0.0
    mapping:
        paths:
            - '%kernel.project_dir%/demosplan/DemosPlanCoreBundle/ApiResources'
            - '%kernel.project_dir%/demosplan/DemosPlanCoreBundle/Api'
    formats:
        jsonld: ['application/ld+json']
        json: ['application/json']
        jsonapi: ['application/vnd.api+json']
    docs_formats:
        jsonld: ['application/ld+json']
        jsonopenapi: ['application/vnd.openapi+json']
        html: ['text/html']
    defaults:
        stateless: false
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
        extra_properties:
            standard_put: true
            rfc_7807_compliant_errors: true
    use_symfony_listeners: false
    serializer:
        # Preserve the current JSON-LD output; api-platform 4 defaults this to false.
        hydra_prefix: true
    validator:
        # Opt out of the deprecated query-parameter validation system (removed in api-platform 5.0).
        # We declare no ApiResource query parameters, so the ParameterValidator is unused.
        query_parameter_validation: false
